# Reprise du travail — état au 17/09/2026

Note de passage d'un poste à un autre. Elle ne décide rien : elle dit où en est le travail, ce qui vient ensuite, et ce qui attend une réponse d'Arnaud. Le détail vit dans les fichiers cités.

## Où en est le projet

`dev` à `2e72c6c`, identique sur la forge et sur le miroir GitHub.

| Epic | État |
| --- | --- |
| 0 — Outillage de développement | **close**, rétrospective faite (`epic-0-retro-2026-09-15.md`) |
| 1 — Garde-fou public/privé avant tout miroir | **close**, rétrospective faite (`epic-1-retro-2026-09-16.md`), 5 stories |
| 2 — Site bilingue et cas pilote | **en cours** : 2.1 à 2.5 `done`, 2.6 et 2.7 à faire |

Le site se construit en deux langues, sans avertissement : 4 pages publiées (`/`, `/en/`, les deux 404) et 6 en rendu de travail, dont la page Chiliz qui porte le cas pilote.

## Ce qui vient ensuite

1. **Story 2.6** — numéros de rubrique et sommaire : le hook de titres d'AD-4 (`02.1`, `02.2`… et le sommaire en `<details>` natif).
2. **Story 2.7** — poste Chiliz en brouillon et lien « retour au parcours ».
3. **Rétrospective de l'epic 2**, une fois les deux livrées.

Le cycle est toujours le même : revue de spec (`scripts/llm-review.sh --story <n.m>`), triage dans le fichier de story, questions à Arnaud, implémentation, revue de code sur la PR, commit de statut `done`, audit, puis fusion **sur son autorisation explicite**.

## Ce qui attend une réponse

Rien ne bloque : aucune question ouverte à la date de cette note.

## Ce que la rétrospective de l'epic 2 aura à examiner

Constats déjà écrits dans les fichiers de story, à ne pas perdre :

- **deux anticipations de la story 2.2**, posées pour qu'un build puisse tourner, et leurs suites opposées : le stub `live-material`, complété sans dommage par la 2.5 comme prévu ; la cascade `render: never` sur `content/cases/_index`, qui rendait invisible la page de groupe et qu'il a fallu défaire deux stories plus loin ;
- **trois clés de configuration** que le cadrage supposait et que Hugo 0.166 refuse (`languageDirection`, `cascade._target`, `_build`), toutes attrapées par `--panicOnWarning` ;
- **`--cleanDestinationDir` qui ne nettoie pas** : trouvé après la fusion de la 2.2, corrigé dans sa propre PR (n° 26) ;
- **`git checkout` ne restaure pas un fichier non suivi** : un `draft: false` d'essai a failli publier la page Chiliz (story 2.5) ;
- **quatre boucles de revue sur la story 2.1**, chaque passe trouvant un constat mineur mais réel : une limite a été posée avec Arnaud pour fermer la PR.

## Travail reporté

`_bmad-output/implementation-artifacts/deferred-work.md` — deux entrées ouvertes, toutes deux sur `scripts/ci/install-tools.sh` (nom d'archive non réduit à son nom de base ; arrêt sans `curl`, `tar` ou `sha256sum` non testé), à reprendre à la prochaine story qui touche ce script.

## Actions de rétrospective encore ouvertes

`sprint-status.yaml`, clé `action_items` : les actions 2, 3, 4 et 5 de l'epic 1 (C20 avant publication, repère de taille des PR, extension de la règle « au présent » à `AGENTS.md`, reprise des actions héritées de l'epic 0).

## Monter le poste

`docs/procedures/poste-de-developpement.md` : ce qui ne vient pas avec le dépôt (`.env`, `docs/private/`, `.tools/`, outils du poste), l'ordre des étapes, et cinq vérifications dont la sortie attendue est écrite.
