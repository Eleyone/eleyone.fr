---
scope: Site statique eleyone.fr : dépôt, Hugo, modèle de contenu et gabarits, pipeline D2, build et déploiement, CI double Gitea/GitHub, contrôles, garde-fou, agent de parité
purpose: build-substrate
altitude: initiative
updated: 2026-09-13T22:04
---

- (event) Run headless (agent sans utilisateur) : entrées = PRD draft, brief et addendum validés, docs/format-cas.md v0.2, data/stack.yaml, cas pilote 02, branche experiment/d2-bilingue
- (version) Hugo v0.166.0 (release GitHub gohugoio/hugo, 09/09/2026), binaire standard linux-amd64 suffisant (pas de SCSS) ; clés de langue label/locale (languageName/languageCode dépréciés en v0.158.0) ; nouveau système de gabarits depuis v0.146.0 (_partials, _shortcodes, home/page/section.html) ; .Site.Data et .Site.AllPages dépréciés en v0.156.0 (hugo.Data, hugo.Sites)
- (version) D2 v0.9.0 (release d2lang/d2 du 07/09/2026, dernière au 13/09/2026), même version que le test D2 bilingue
- (version) nginx image officielle : stable 1.30.4-alpine (alpine 3.24), mainline 1.31.5 (docker-library/official-images)
- (version) Gitea 1.27.x (1.27.0 le 12/07/2026, doc 1.27.3), Gitea Runner 2.0.0 requis pour continue-on-error correct et résumés de job
- (constraint) Gitea Actions : setting.Actions.WorkflowDirs, clé [actions] WORKFLOW_DIRS, défaut [.gitea/workflows, .github/workflows] ; listWorkflowsInDirs retient le PREMIER dossier existant (source modules/actions/workflows.go, main) → si .gitea/workflows existe, .github/workflows est ignoré par Gitea ; GitHub ne lit que .github/workflows
- (constraint) Gitea hooks : DISABLE_GIT_HOOKS ([security], défaut true depuis 1.13) ne conditionne que l'édition des hooks par l'UI (User.CanEditGitHook) ; le hooks/pre-receive généré exécute tous les exécutables de hooks/pre-receive.d/ (le hook Gitea est pre-receive.d/gitea) ; la régénération réécrit pre-receive et pre-receive.d/gitea sans supprimer les autres fichiers ; SCRIPT_TYPE défaut bash
- (constraint) Gitea Actions secrets : pas de secrets pour une PR de fork sauf événement pull_request_target (models/secret/secret.go) ; GITEA_TOKEN : permissions: supporté (scopes issues, pull-requests…), modes permissive/restricted, fork = lecture seule (doc 1.27.3) ; filtres paths et paths-ignore supportés
- (constraint) GitHub : ubuntu-latest = x64 pour dépôts publics (docs runners hébergés) ; arm64 seulement via labels -arm → rendu D2 testé sur x86_64 applicable
- (constraint) Miroir push Gitea → GitHub natif, option synchro à chaque push depuis Gitea 1.18 (doc repo-mirror)
- (version) Claude API (skill claude-api, cache 24/06/2026) : claude-opus-5 5 $/25 $ par MTok, claude-sonnet-5 2 $/10 $, claude-haiku-4-5 1 $/5 $ ; appel HTTP brut : POST /v1/messages, en-têtes x-api-key, anthropic-version 2023-06-01
- (event) Spike Hugo v0.166.0 hors dépôt (scratchpad) avec le cas pilote 02 : rendu production et rendu de travail
- (decision) Cas groupés : dossier content/cases/<group>/ dont _index.{fr,en}.md est la page de groupe ; cascade build {render: never, list: always} avec target {kind: page} ; spike : sans target, la cascade supprime aussi la page de groupe (constaté v0.166.0) ; avec target, pas de page séparée pour case-02, page /cas/chiliz/ et /en/cases/chiliz/ rendues, cas listé dans site.RegularPages ; empêche : pages séparées pour 02/03/04 et page de groupe à modifier à chaque cas
- (decision) Permalinks par langue : fr page cases /cas/:slug/, section /cas/:sections[1:]/ ; en /cases/:slug/ (préfixe /en/ automatique), section /cases/:sections[1:]/ ; disableDefaultSiteRedirect: true supprime l'alias /fr/ (meta refresh) constaté dans le spike ; 404.html rendu par langue (/404.html, /en/404.html)
- (decision) Ancre de section = translationKey (case-02), identique FR/EN ; lien vers un cas groupé = Parent.RelPermalink + #translationKey (spike : /cas/chiliz/#case-02 depuis l'accueil)
- (decision) live-material : shortcode rend l'élément si status ready, ou si hugo.IsProduction est faux ; spike : en production aucune trace (ni figure, ni <p> vide) ; en rendu de travail (--environment work --buildDrafts) figure visible ; id non déclaré → errorf (build en échec)
- (decision) Page de groupe sans cas publié : spike montre une page rendue avec seulement <h1> en production → contrôle bloquant sur le build de production (groupe sans section publiée = échec), ce qui cale la première mise en ligne sur le socle
- (decision) Contrôles de contenu : Hugo émet un manifeste JSON (format de sortie checks sur home) seulement dans l'environnement work via config/work/hugo.yaml ; spike : absent du build production ; front matter lu par Hugo (un seul parseur YAML), titres H2 extraits du Markdown brut (les titres rendus passent par la typographie : &rsquo;)
- (constraint) D2 v0.9.0 : SVG écrit en 0600 même avec umask 022 ; 2 éléments <style> et 2 @font-face embarqués ; aucun <title>/<desc> ; svg interne width/height (876x1344) → alt via <img>, dimensions extraites du SVG ; rendu identique à l'octet au SVG commité de l'expérience
- (decision) CSP servie uniquement sur les réponses text/html (les SVG embarquent <style> et polices data:) ; default-src 'none' sans script-src : le zéro JS est aussi imposé côté navigateur
- (decision) Paradigme : pipes-and-filters de génération statique ; sources (content, data, i18n, diagrams) → filtres (render D2, contrôles, Hugo) → public/ → image nginx → production ; le contenu ne connaît pas la mise en page
- (decision) Versions épinglées à un seul endroit : ARG du Dockerfile (Hugo, D2, nginx) ; binaires téléchargés avec sha256 ; les scripts natifs vérifient la version et échouent si elle diffère
- (decision) CI double : scripts/check.sh seul point d'entrée des contrôles ; .gitea/workflows (checks, parity-agent, release) et .github/workflows/checks.yaml minces ; Gitea ignore .github dès que .gitea/workflows existe (vérifié dans le code) ; GitHub ubuntu-24.04 x64 ; contrôles exécutés dans docker build --target checks sur les deux forges
- (decision) Contrôles de mise en ligne (--release, workflow release seulement) : groupe sans section publiée, pages du socle présentes, cas mis en avant non brouillons ; la CI courante tolère le pilote en brouillon
- (decision) Déploiement recommandé (à valider) : tag v* sur Gitea → contrôles → image → docker save | ssh vers commande forcée deploy-site sur le serveur de production (docker load + redémarrage, 3 images gardées pour rollback) ; pas de registre ; homelab arrêté = pas de déploiement, le site tourne ; plan B : même script depuis le poste, après check-private history
- (decision) Garde-fou : pre-commit local (staged) ; pre-receive Gitea par fichier hooks/pre-receive.d/check-private dans le dépôt nu, script et motifs hors dépôt sur le serveur, fail-closed sans fichier de motifs (modification du script à faire) ; DISABLE_GIT_HOOKS reste true ; CI des deux forges : chemins interdits sur tout l'historique ; aucun push direct vers GitHub
- (decision) Agent de parité (recommandé) : script bash+curl+jq vers l'API Messages, déclenché par pull_request (jamais pull_request_target) avec filtre paths de contenu, continue-on-error et sortie 0, clé ANTHROPIC_API_KEY secret Gitea, commentaire via POST /api/v1/repos/{owner}/{repo}/issues/{index}/comments avec GITEA_TOKEN ; modèle claude-opus-5 par défaut en variable (≈0,08 $ par paire de cas) ; pas sur GitHub ; écartés : Claude Code CLI (Node, agentique, coût moins prévisible), action GitHub dédiée (GitHub seulement)
- (decision) Accessibilité : contrôles HTML automatiques par script (lang, alt, titres, liens, hreflang, dimensions img, pas de tabindex positif) + check-list manuelle par gabarit WCAG 2.2 AA (clavier, focus visible et non masqué, reflow 320 px, contraste mesuré, taille des cibles 24 px) ; INP argumenté par zéro script + budget DOM + une mesure manuelle à la mise en ligne du socle
- (assumption) Budget de poids proposé (à valider) : HTML ≤ 50 Ko, CSS total ≤ 20 Ko, SVG ≤ 60 Ko chacun, page ≤ 200 Ko non compressé, ≤ 10 requêtes, 0 JS, 0 police web, ≤ 800 éléments DOM
- (assumption) Propositions à valider : libellés EN (Engagement context, At a glance, Employee, Freelance, IT consultancy, Ton Pote le Geek) ; préfixes d'URL /cas/ et /en/cases/ ; locales fr et en ; x-default vers le FR ; police système ; README en anglais ; déploiement par tag ; modèle de l'agent
- (decision) Walking skeleton : 5 tranches sur le seul cas pilote (config bilingue + base, page Chiliz + encarts + shortcode, contrôles v1, Dockerfile + nginx, workflows des deux forges), démontrées sur le rendu de travail
- (direction) Décisions d'Arnaud du 13/09/2026 transmises en cours de run : mentions légales par l'environnement, journaux nginx sans donnée personnelle, reverse proxy existant = Nginx Proxy Manager (aucune IP journalisée sur toute la chaîne pour ce site)
- (decision) Mentions légales : valeurs lues par os.Getenv (liste d'autorisation par défaut ^HUGO_ et ^CI$, doc security) dans un seul partial, variables HUGO_LEGAL_* ; partial en échec si valeur vide ; .env local ignoré, .env.example avec les noms seulement, ci/legal-placeholder.env avec valeurs factices marquées pour les builds de contrôle ; contrôle de mise en ligne : marqueur factice absent de public/ ; build d'image : secret BuildKit, .env exclu du contexte Docker et ajouté aux chemins interdits
- (constraint) Spike Hugo v0.166.0 : HUGO_PARAMS_LEGAL_PUBLISHER_NAME ne remplit pas params.legal.publisher_name (découpage sur _) ; HUGOxPARAMSxLEGALxPUBLISHER_NAME fonctionne ; HUGO_PARAMS_LEGAL_SIRET crée la clé ; os.Getenv HUGO_LEGAL_ADDRESS fonctionne ; os.Getenv d'une variable hors ^HUGO_ = erreur access denied ; variable absente = chaîne vide
- (decision) Contrôles hors Docker : scripts/ci/install-tools.sh installe Hugo et D2 depuis tools.env (versions + sha256), utilisé par les deux CI et par l'étape tools du Dockerfile ; GitHub fait un build Hugo de contrôle, jamais de docker build ; remplace l'idée docker build --target checks
- (decision) Journaux du conteneur du site : log_format sans IP ni user-agent ni referer ( ) vers stdout, error_log warn vers stderr, log_not_found off, pas de module realip (le conteneur ne voit que l'IP du proxy), rotation json-file max-size/max-file (Docker : pas de rotation par défaut) ; limitation de débit reportée
- (constraint) NPM (dépôt NginxProxyManager develop, release v2.15.1 du 03/06/2026) : proxy_host.conf déclare access_log proxy-host-<id>_access.log (format proxy avec $remote_addr, user-agent, referer) et error_log warn au niveau server, puis {{ advanced_config }} ; une location / dans la config avancée supprime la location par défaut (advancedConfigHasDefaultLocation) ; nginx : access_log off annule les access_log du même niveau ; aucun réglage NPM natif pour couper les journaux (discussion #2170 sans réponse de mainteneur)
- (decision) NPM pour ce site (à vérifier par Arnaud) : access_log off; dans la configuration avancée de l'hôte ; journal d'erreurs du serveur : ne peut pas être annulé au même niveau, contournement incertain par location / personnalisée avec error_log /dev/null crit ; résiduel possible pour les erreurs de niveau server (TLS, requêtes invalides)
- (assumption) Tension : NFR-9 (aucun fichier public ne contient un lieu) contre adresse de l'éditeur dans la page légale publique ; décision d'Arnaud = hors dépôt, présente dans le HTML ; formulation de NFR-9 à mettre à jour dans le PRD
- (event) Reviewer gate lancé : lint_spine = 3 high (mot TODO = marqueur de contenu [TODO du format, faux positif assumé) + 4 low (jetons {variables}, {owner}, {repo}, {index} = syntaxe D2 et chemin d'API, faux positifs) ; grep des motifs privés sur _bmad-output : aucune occurrence ; relecteurs parallèles : rubrique, vérification web, adversarial, réconciliation des entrées
- (change) AD-15 NPM réécrit d'après la recherche du coordinateur (code NPM v2.12.6 à v2.15.1) : points classés confirmé / à tester ; access_log off en Advanced (confirmé) ; error_log serveur additif (confirmé) ; Custom Location / avec error_log /dev/null crit (à tester) ; map d'anonymisation dans http_top.conf (à tester) ; à éviter : location / dans Advanced, édition du .conf généré ; traces fallback_* et default-host, purge des anciens journaux ; le site n'utilise ni X-Real-IP ni X-Forwarded-For dans ses journaux ; vérification nginx -t / nginx -T
- (change) Revue vérification web appliquée : (V-1) outils de contrôle non fournis par les runners (pas de xmllint sur ubuntu-24.04, libxml2 2.9.14 vs 2.13.9) → contrôles dans un conteneur CHECK_IMAGE alpine:3.24 épinglé par digest dans tools.env, lancé par docker run depuis checks-job.sh (pas de build d'image) ; (V-2) nginx add_header ... always (sinon absent des 404), add_header_inherit merge (nginx 1.29.3+), map ~^text/html, suppression d'en-tête sur valeur vide à vérifier par curl -I en WS-5 ; (V-4) secret BuildKit explicite src= + required=true ; (V-5) un seul scripts/build.sh production pour contrôle et image (--minify retire les guillemets), attributs vérifiés en XPath ; dépréciations Hugo en INFO pendant 3 versions mineures ; 2.5.8 exemption des liens en ligne
- (change) Réconciliation des entrées appliquée : miroir GitHub après hook et audit ; CHECK_LEVEL ; --no-cache-filter build ; D2 retours à la ligne manuels, thème Neutral Grey sans --theme, vars comparées à structure.d2, schéma de démo dans tests/fixtures ; render-heading.html (ids préfixés, niveau décalé) ; résiduel referrer du journal d'erreurs nginx à valider ; limitation de débit seulement dans NPM ; Cache Assets désactivé, deploy/proxy/npm-advanced.conf ; agent ignore les lignes de contexte EN ; checks.yaml sur push main + pull_request ; C18 règles du format ; questions 7 et 8 reportées ; hypothèse serveur amd64 ; baseURL, disableKinds, sitemap
- (change) Revues rubrique et adversariale appliquées : scripts/build.sh work|production seul appel hugo, sorties build/work et public avec --cleanDestinationDir ; WS-0 garde-fou avant miroir ; secret monté dans le même RUN ; check.sh sans .git ; manifeste de tous les fichiers avec kind et rôle, forme dans home.checks.json et lib.sh, portée des contrôles, translationKey home et cases ; planned jamais résolu en travail ; ids de matériel identiques FR/EN et propres à un cas, <lang> = clé de langue ; shortcode legal et chargeur scripts/env.sh (ENV_MODE=release) ; label runs-on unique Gitea ; tag v semver sur main ; protocole deploy-site (deploy/rollback/status via SSH_ORIGINAL_COMMAND) ; procédure de premier déploiement ; ci/release-pages.txt ; C16 reporté (summary replié) ; 404 exclues des orphelins ; Gitea Runner 2.2.0 publié
- (direction) Précision d'Arnaud : hôte proxy NPM du site pas encore créé, domaine non pointé → aucune purge de journaux ; configuration sans IP dès la création de l'hôte, étape du premier déploiement avec vérification ; fallback_* et default-host_* seulement en point de vigilance
- (event) Gate terminé : 4 revues appliquées ; lint final = 4 high (mot TODO du marqueur de contenu) + 4 low (jetons D2 et chemin d'API), faux positifs ; grep des motifs privés sur _bmad-output (revues incluses) : aucune occurrence ; statut du spine laissé draft en attente de validation par Arnaud ; aucun commit ; .gitignore et scripts/check-private.sh modifiés hors de ce run (ajout de .env, 17:50), laissés en l'état
- (override) Décisions d'Arnaud du 13/09/2026 intégrées : validés tels que proposés = budget de poids, libellés EN, police système, URL /cas/ et /en/cases/ avec locales fr/en et x-default FR, format v0.3 (cas groupés dans content/cases/<group>/), tag vX.Y.Z + SSH sans registre + 3 images, README-cas en anglais ; tranché autrement = agent de parité sur claude-sonnet-5 (≈0,03 $ par paire, estimation), error_log du site en crit ; format v0.3 et pilote déplacé annoncés faits par le coordinateur (non constatés dans la copie de travail dev au moment de la mise à jour) ; restent ouverts = variables HUGO_LEGAL_*, mesure des trois lignes (Q11), slugs des pages simples, application et tests NPM ; statut draft conservé
- (event) Correction : docs/format-cas.md en v0.3 et pilote dans content/cases/chiliz/ constatés dans la copie de travail (modifications non commitées) juste après la mise à jour ; la réserve de l'entrée précédente est levée
- (override) Recommandation 10 validée par Arnaud (13/09/2026) : sept variables légales définitives (HUGO_LEGAL_PUBLISHER_NAME, _ADDRESS, _CONTACT, _REGISTRATION, HUGO_LEGAL_HOST_NAME, _ADDRESS, _CONTACT) ; directeur de la publication = éditeur, affiché via HUGO_LEGAL_PUBLISHER_NAME sous le libellé i18n legal_publication_director ; pas de TVA intracommunautaire ; AD-9, secrets et variables CI Gitea, .env.example (sept noms sans valeur) et C18 mis à jour ; recommandation retirée des points ouverts
- (change) Renvois aux questions du PRD alignés sur le §11.2 figé (Q1 à Q14) : Q6, Q7, Q8, Q9 et Q11 inchangés ; étape UX 15→12 ; v1.1 16→13 ; mentions légales, journaux et langue du README marqués ex-questions 12, 13 et 14 tranchées ; tension NFR-9 marquée résolue (NFR-9 reformulée dans le PRD)
- (override) Dernières validations d'Arnaud (13/09/2026) : slugs des pages simples (/a-propos/, /contact/, /mentions-legales/, /confidentialite/ ; /en/about/, /en/contact/, /en/legal-notice/, /en/privacy/) ; C16 bloquant = 3 phrases et 400 caractères au plus par langue sur summary (question 11 tranchée ; pilote FR 354, EN 346, 3 phrases) ; configuration NPM = étape du premier déploiement, dès la création de l'hôte, Custom Location et Cache Assets à tester lors de ce déploiement ; plus aucune recommandation ouverte
- (direction) Décisions d'Arnaud du 13/09/2026 (lot 3) : accueil = CV (parcours du plus récent au plus ancien, cas par poste, bloc En parallèle avec Ton Pote le Geek et cas 01, bloc formation, critère mobile 390x844, mode sombre CSS pur) ; identité (nom et pseudonyme, Basé en France, nom dans <title>) ; photo publiée sans métadonnées ; JSON-LD Person comme seule exception au zéro JS ; CV PDF FR/EN statiques en v1 avec contrôle bloquant, génération Typst en v1.1 ; NFR-9 : nommer docs/private autorisé ; contact (mail, LinkedIn) commité ; répétition à blanc de la mise en ligne ; villes de travail publiables, seule la ville de résidence est privée ; tensions techniques des stories à corriger ; aucun détail visuel
- (constraint) Spike Hugo v0.166.0 : JPEG source avec EXIF GPS (lat lisible par .Exif) ré-encodé par .Process (resize webp, resize jpg, webp) → sorties sans Exif, XMP ni GPS (grep octets) ; WebP produit en VP8 simple (sans VP8X, donc sans bloc de métadonnées) ; doc Hugo : métadonnées non conservées à la transformation ; WebP en édition standard depuis v0.153.0 (libwebp WASM)
- (constraint) Alpine 3.24 : poppler-utils 25.12.0-r1 contient pdftotext et pdfinfo (pkgs.alpinelinux.org) ; Typst 0.15.0 (15/06/2026) : --creation-timestamp ou SOURCE_DATE_EPOCH, correctif du fuseau local pour des PDF reproductibles ; CSP : <script type=application/ld+json> est un bloc de données, non exécuté, non concerné par script-src (sources secondaires) ; GitHub Actions : actions JavaScript dans un conteneur Alpine fragiles (node lié à glibc) → docker run depuis le runner plutôt que job container
- (decision) AD-18 parcours : contenu Markdown content/career/position-<id> et content/education/education-<id> (headless, cascade render never), translationKey stable, order explicite (pas de dates calculées), period texte d'auteur, city et/ou setup, track main|parallel ; lien cas → poste par clé position dans le cas (format v0.4 proposé) : publication d'un cas sans toucher au poste, brouillons exclus par Hugo ; C19
- (decision) AD-19 identité et photo : identity, based_in, portrait_alt dans content/_index ; <title> via baseof ; original hors dépôt ; scripts/photo/prepare.sh ré-encode par le Hugo épinglé (resize 800 webp q80) vers assets/images/portrait.webp ; variantes 1x/2x par .Process ; C20 contrôle d'octets (Exif, XMP, GPS, VP8X) et budgets proposés (≤150 Ko commité, ≤40 Ko publié)
- (decision) AD-20 JSON-LD Person : seul script accepté, type exact application/ld+json sans src, un par page, JSON valide @type Person, sans ville ni téléphone ; CSP inchangée ; ADR-14
- (decision) AD-21 CV PDF : v1 static/cv/cv-fr.pdf et cv-en.pdf ; C21 pdf.sh (poppler) présence/en-tête/pages/≤500 Ko partout, texte+métadonnées+XMP contre la liste des motifs sur poste et job release (secret PRIVATE_PATTERNS) ; git grep -I ignore les binaires → garantie serveur seulement avec poppler dans l'image Gitea (à valider) ; v1.1 Typst 0.15.0 avec SOURCE_DATE_EPOCH, PDF commités et comparés après spike
- (decision) AD-22 répétition à blanc : projet compose site-rehearsal sur le serveur de production, hors réseau NPM, 127.0.0.1:18080, accès par tunnel SSH ; tags vX.Y.Z-rc.N ; deploy-site rehearse deploy|rollback|stop ; jalon répétition générale avant v1.0.0 (rc.1, rc.2, rollback, stop)
- (change) Tensions des stories corrigées : brouillons exemptés des règles de forme (AD-10, C18) ; C12 avec en-tête (accueil, à propos, contact) et pied de page (légales, dépôt public, CV) ; mesure PageSpeed consignée dans docs/measures/<tag>.md ; env.sh lit LEGAL_ENV_FILE=/run/secrets/legal_env en mode release ; runners Gitea en mode hôte (espace de travail en volume Docker en mode conteneur) ; renvois ex-questions remplacés par PRD §11.1 ; étape .env retirée de WS-0 ; lien vers la politique de l'hébergeur dans le texte ; contact commité ; nommer docs/private autorisé ; mode sombre CSS pur ; critère mobile 390x844 dans la check-list
- (change) Alignement sur le PRD mis à jour (FR-33 à FR-39, NFR-13, questions 15 à 17) : renvois numérotés dans AD-8, AD-17 à AD-22, liste des contrôles et carte des capacités ; FR-36 → content/education avec kind education|certification|language ; FR-35 → champs JSON-LD exacts (name, alternateName, jobTitle, pays, url, sameAs LinkedIn et GitHub), un bloc sur l'accueil seulement, pas de photo ; FR-2 → plus de cas mis en avant, featured à supprimer en v0.4, postes avec location et via ; question 15 → cas 06 sous le poste April Technologies 2017 ; FR-38 → blocage avant l'historique, extraction serveur nécessaire ; questions 16 et 17 ajoutées aux tensions ; questions 6, 7, 8 marquées tranchées
- (direction) Compléments d'Arnaud (13/09/2026) : direction visuelle tranchée côté DESIGN.md/EXPERIENCE.md (aucune valeur visuelle dans l'architecture, dépendance seulement ; un poste sans cas n'affiche rien) ; Q17 option a : commune de l'adresse de l'activité déclarée seulement sur la page des mentions légales, injectée par HUGO_LEGAL_PUBLISHER_ADDRESS, jamais commitée → règle des motifs interdits : sources versionnées et PDF ; sortie de production : valeurs injectées seules autorisées sur les mentions légales ; liens CV PDF (proposition) : pied de page et page À propos, pas l'en-tête, affichés seulement si les fichiers existent et ont passé leur contrôle, socle possible sans eux (réponse proposée à Q16) ; FR-38 : contrôle d'extraction avant l'historique (pre-commit et pre-receive)
- (change) Compléments intégrés : spike Hugo v0.166.0 (resources.Get d'un PDF absent = nil ; asset publié seulement si référencé) → CV PDF déplacés dans assets/cv/, _partials/cv-links.html conditionnel, pied de page et page À propos, socle possible sans PDF (Q16 proposée), C21 accepte aucun ou les deux ; AD-21 extraction avant l'historique (pre-commit, pre-receive avec poppler, release), assets/cv/*.pdf interdit tant que le hook serveur n'extrait pas ; AD-9 Q17 option a, commune seulement sur les mentions légales ; nouveau C22 output-patterns.sh (release) : motifs sur public/, valeurs injectées seules admises sur les mentions légales ; AD-18 poste sans cas = champs sans zone de cas ; direction visuelle renvoyée à DESIGN.md et EXPERIENCE.md, sans valeur
- (override) Validation d'Arnaud (13/09/2026) : liens CV PDF en pied de page et sur À propos, conditionnels (fichier présent et contrôlé) = décision ; questions 16 et 17 du PRD tranchées dans ce sens (PRD mis à jour par John) ; AD-21, décisions (25), recommandations (reste : noms publics des PDF) et écarts alignés
- (override) Huit recommandations validées par Arnaud (13/09/2026) : format v0.4 (position, suppression de featured, appliqué par le coordinateur), parcours en contenu Markdown, budget photo, noms /cv/cv-fr.pdf et /cv/cv-en.pdf, poppler-utils sur la machine Gitea avant tout commit de PDF (assets/cv/*.pdf interdit d'ici là), runners en mode hôte, répétition sur 18080 avec tags -rc.N, champs location et via ; poste sans cas affiché sans zone de cas ; procédure pre-receive étape 7 détaillée pour Gitea sur la machine et en conteneur (image officielle sur alpine:3.24, image dérivée avec apk add poppler-utils, reconstruite à chaque montée de version) ; aucune recommandation ouverte
- (change) UX validée (DESIGN.md, EXPERIENCE.md, impacts I-1 à I-11) intégrée sans valeur visuelle : AD-2 libellés du sélecteur ; AD-3 lien du dépôt conditionnel (params.source_url) ; AD-4 numéros de rubrique NN.r par .PageInner.Fragments.Headings (spike : 02.1 à 02.6), aria-hidden, sommaire <details> ; AD-5 marqueur Brouillon (draft-marker) en rendu de travail, absent de public/ (C15) ; AD-6 schéma large sans style en ligne ; AD-7 spike D2 à double thème avant le pipeline (img, octets, 60 Ko), repli planche claire ; AD-9 legal-value.html refuse toute lecture hors legal-notice, C23 adresse confinée ; AD-18 ancres position, retour au parcours (career-url.html), cas sans En bref sous un poste, corps rendu seulement sans cas ; AD-19 crop 4:5 640x800 avec ancrage manuel sans Smart, variantes 120x150/240x300 et 160x200/320x400 ; AD-21 CV ensemble ou rien ; nouvel AD-23 typographie française au build (partial typo-fr, C24)
- (decision) AD-24 flux de développement (décision d'Arnaud du 13/09/2026) : feat/* et fix/* depuis dev en squash, dev → main en merge commit, tags vX.Y.Z sur main et vX.Y.Z-rc.N sur dev (plutôt que release/*), dev et main protégées (main à créer), défaut main sur GitHub ; style de fusion imposé par verify-and-merge-pr (API do squash|merge, head_commit_id) car Gitea 1.27 ne le fixe pas par branche ; outillage skill → procédure → script pour 8 outils ; llm-review via agy --mode plan avec modèle d'un autre fournisseur, worktree temporaire hors dépôt + check-private avant envoi (plan read-only non documenté, identifiant du modèle par agy models) ; rapport en commentaire de PR (à valider) ; verrous rapport/garde-fou/CI/sprint, audit par défaut, --merge explicite, jamais --force ; exception documentaire .md de _bmad-output ; README documente le flux et les branches design/* et experiment/d2-bilingue (celle-ci seulement locale) ; AD-11, AD-14, AD-22 alignés ; ADR-18, ADR-19
- (override) Correction d'AD-24 (Arnaud, 13/09/2026) : aucun merge commit ; publication dev → main linéaire ; hotfix fix/* depuis main, réintégré dans dev. Propositions de la table marquées à valider : fast-forward-only (Gitea 1.22.0+, champ do) avec invariant main ancêtre de dev ; hotfix = PR ff vers main, tag vX.Y.(Z+1), rebase de dev sur main + --force-with-lease (force-push de dev réservé à Arnaud), rebase des feat/*, pas de cherry-pick ; feat/* → dev en squash ; miroir : main jamais réécrite, dev remplacée après hotfix ; verify-and-merge-pr refuse la base main, release vérifie l'ancêtre, nouveau skill hotfix (9 outils). AD-11 (push dev et main), AD-14 (tag sur main après ff ou hotfix), AD-22 (rc sur dev, arbre identique pour v1.0.0), ADR-18 alignés
- (override) Confirmations d'Arnaud (13/09/2026) sur AD-24 : fast-forward-only pour dev → main (échec si main n'est pas ancêtre de dev) ; après hotfix, rebase de dev sur main et --force-with-lease, faisable par le skill hotfix seulement après approbation explicite d'Arnaud au moment de l'opération, force-push sur dev limité à son compte dans Gitea ; feat/* → dev en squash ; skill hotfix ajouté. Restent à valider : miroir (dev remplacée sur GitHub après hotfix), refus de la base main par verify-and-merge-pr et vérification d'ancêtre par release, rapport llm-review en commentaire de PR, rc sur dev et arbre identique pour v1.0.0, vérification agy, push de experiment/d2-bilingue
- (assumption) Authentification des skills auprès de l'API Gitea (proposition, à valider par Arnaud) : constat du 13/09/2026 sur le poste, remotes en SSH, aucun credential.helper ni ~/.git-credentials, git credential fill vide pour l'URL HTTPS de la forge ; proposition : jeton d'accès personnel Gitea aux portées minimales avec expiration, rangé hors dépôt dans le magasin d'identifiants git (store, fichier 600), lu par git credential fill sans affichage ; .env écarté (visible par un agent) ; worktree de llm-review sans identifiants ; push toujours en SSH ; création manuelle par Arnaud dans son terminal, prérequis de create-pull-request, verify-and-merge-pr, release et hotfix
- (override) Derniers points validés par Arnaud (13/09/2026) : rapport llm-review en commentaire de PR ; rc sur dev et v1.0.0 refusé sans rc de même arbre ; vérification agy = critère d'acceptation de la story llm-review ; experiment/d2-bilingue poussée ; miroir (main jamais réécrite, dev remplacée après hotfix), refus de la base main par verify-and-merge-pr et vérification d'ancêtre par release passés en décidé ; authentification Gitea par jeton personnel dans le magasin d'identifiants git hors dépôt, lu par git credential fill, décidée ; aucune recommandation ouverte
- (override) Correction d'Arnaud (13/09/2026) sur l'authentification Gitea : tout jeton, identifiant ou secret du projet est défini dans .env à la racine du dépôt, jamais commité ; magasin d'identifiants git abandonné ; variables GITEA_URL, GITEA_USER, GITEA_TOKEN (portées minimales, expiration) ; scripts chargent .env sans afficher de valeur et échouent avec renvoi à la procédure si une variable manque ; .env ignoré, chemin interdit du garde-fou, absent du worktree de llm-review ; .env.example avec les noms seulement ; en CI, secrets Gitea ; proposition à valider : interdire aux agents de lire ou d'afficher .env (refus de permission Claude Code, consigne AGENTS.md)
- (change) AD-24 et AD-9 alignés sur .env : jeton Gitea (GITEA_URL, GITEA_USER, GITEA_TOKEN) défini dans .env comme tous les secrets locaux ; env.sh ne transmet à Hugo que HUGO_LEGAL_* ; .env.example avec tous les noms (C18) ; vérifié (doc Gitea 1.27) : secrets CI interdits avec les préfixes GITEA_ et GITHUB_, jeton automatique GITEA_TOKEN en CI → les variables GITEA_* restent locales ; recommandation ouverte : interdire aux agents de lire ou d'afficher .env
- (change) AD-24 : jq absent du poste (constat du 13/09/2026) → recommandation à valider : installation manuelle par Arnaud (sudo apt install jq), prérequis des procédures du poste, vérification command -v jq dans chaque script, pas de portage en python3 ; AD-10 : les contrôles tournent dans CHECK_IMAGE qui embarque jq, pas de jq requis sur le poste via checks-job.sh. Refus de lecture de .env pour les agents décidé : .claude/settings.json permissions.deny (Read, Edit, cat/less/head/tail/grep, env, printenv), réglage Antigravity du poste (read_file, write_file, docs/private, command), .antigravityignore en défense en profondeur (issue #309), consigne AGENTS.md ; limites : refus de fichier contournable par le shell, résistance à --dangerously-skip-permissions non documentée ; garantie = worktree isolé
