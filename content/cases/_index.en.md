---
translationKey: cases
# Section technique : jamais rendue, jamais listée (arborescence d'AD-2). La page de groupe Chiliz et
# les pages de cas viennent des stories 2.4 et 2.5 ; la cascade ne vise que les sections, pas les pages.
build:
  render: never
  list: never
cascade:
  - target:
      kind: section
    build:
      render: never
      list: never
---
